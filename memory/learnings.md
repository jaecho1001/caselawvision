# Learnings — self-improvement journal

> Last updated: 2026-07-14.

Append-only. One dated bullet per lesson: what happened, what you learned, and (if it became a
rule) where you wrote it down.

- 2026-07-14 — Bootstrapped the agent brain onto the `caselawvision` bible repo. Kept `docs/`
  and `README.md` fully intact and added the brain as an additive layer; the docs stay
  authoritative for the technical contract while `AGENTS.md` + `memory/` onboard agents.
  → *(rule in `conventions.md`)*
- 2026-07-14 — `caselawvision` and `caselawvision-platform` are BOTH "CaseLawVision platform
  bibles" for the same platform, and they diverge (app status, `ix_*` table names, scope —
  `caselawvision-platform` also carries `brain-kit/` + `IMPLEMENTATION.md` and a fifth app,
  DivorceMate). Rather than assume this repo duplicates the other, recorded the overlap as an
  explicit Open Question / decision (possible consolidation). → *(decision in `decisions.md`,
  rule in `conventions.md`)*
- 2026-07-14 — This repo's `README.md` marks four apps "Active" with public repo links, which
  conflicts with `caselawvision-platform`'s "Built/private/Planned" statuses. Treated README app
  status as possibly aspirational and flagged it for per-app confirmation rather than asserting
  it. → *(gotcha in `conventions.md`)*
